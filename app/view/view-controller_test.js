'use strict';

describe('musicListAppViewController module', function() {

  beforeEach(module('musicListAppViewController'));

  describe('ViewController controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      //var viewController = $controller('ViewController');
      //expect(viewController).toBeDefined();
    }));

  });
});